import { Router } from "express";
declare class ContactRouter {
    private route;
    private contactController;
    constructor();
    private initializeRoutes;
    getRouter(): Router;
}
export default ContactRouter;
//# sourceMappingURL=contact.router.d.ts.map