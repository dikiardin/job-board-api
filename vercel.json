{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",  // Only build the main entry point
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }  // Route everything to the main function
  ]
}
